defmodule LabelingIR do
  @moduledoc """
  Shared IR structs for the labeling stack (Forge, Anvil, Ingot, external clients).

  This module only documents the library; concrete structs live under
  `LabelingIR.*` modules.
  """
end
